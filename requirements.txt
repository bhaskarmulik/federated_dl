# FALCON Python dependencies
# ===========================
# Core (picograd only needs numpy)
numpy>=1.24.0

# Dashboard backend (optional — only needed to run the web dashboard)
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
eventlet>=0.35.0
scipy
# Dataset (optional — only needed for MedMNIST experiments)
# medmnist>=3.0.0
# Pillow>=10.0.0

# Testing utilities (optional)
# pytest>=7.4.0

-e .